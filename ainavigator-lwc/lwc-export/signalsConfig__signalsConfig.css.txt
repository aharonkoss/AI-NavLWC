.sc-wrapper {
    padding: 24px 28px;
    background-color: #f3f4f6;
    min-height: 100vh;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

/* Header */
.sc-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 16px;
}
.sc-title {
    margin: 0;
    font-size: 1.25rem;
    font-weight: 700;
    color: #111827;
}
.sc-subtitle {
    margin: 4px 0 0;
    font-size: 0.85rem;
    color: #6b7280;
}
.sc-header-actions {
    display: flex;
    gap: 10px;
    align-items: center;
}
.sc-btn-reset {
    padding: 8px 14px;
    border: 1px solid #d1d5db;
    border-radius: 8px;
    background: #fff;
    font-size: 0.85rem;
    cursor: pointer;
    color: #374151;
}
.sc-btn-reset:hover:not(:disabled) { background: #f9fafb; }
.sc-btn-save {
    padding: 8px 16px;
    border: none;
    border-radius: 8px;
    background: #1d4ed8;
    color: #fff;
    font-size: 0.85rem;
    font-weight: 600;
    cursor: pointer;
}
.sc-btn-save:hover:not(:disabled) { background: #1e40af; }
.sc-btn-save:disabled,
.sc-btn-reset:disabled { opacity: 0.6; cursor: not-allowed; }

/* Banners */
.sc-banner-success {
    background: #ecfdf3;
    border: 1px solid #bbf7d0;
    color: #166534;
    border-radius: 8px;
    padding: 8px 12px;
    font-size: 0.85rem;
    margin-bottom: 14px;
}
.sc-banner-error {
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #b91c1c;
    border-radius: 8px;
    padding: 8px 12px;
    font-size: 0.85rem;
    margin-bottom: 14px;
}

/* Loading */
.sc-loading {
    display: flex;
    gap: 8px;
    align-items: center;
    font-size: 0.85rem;
    color: #4b5563;
    margin-top: 20px;
}
.sc-spinner {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    border: 2px solid rgba(55,65,81,0.3);
    border-top-color: #111827;
    animation: spin 0.8s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* Summary card */
.sc-summary-card {
    background: linear-gradient(135deg, #1d4ed8, #3b82f6);
    border-radius: 12px;
    padding: 20px 24px;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 20px;
    color: #fff;
}
.sc-summary-label {
    margin: 0 0 4px;
    font-size: 0.8rem;
    opacity: 0.85;
}
.sc-summary-count {
    margin: 0 0 10px;
    font-size: 1.8rem;
    font-weight: 700;
}
.sc-priority-legend {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-wrap: wrap;
}
.sc-legend-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    display: inline-block;
}
.sc-dot-p5 { background: #ef4444; }
.sc-dot-p4 { background: #f59e0b; }
.sc-dot-p3 { background: #eab308; }
.sc-legend-text {
    font-size: 0.75rem;
    opacity: 0.9;
    margin-right: 8px;
}
.sc-summary-right { text-align: right; }
.sc-priority-badges {
    display: flex;
    gap: 6px;
    margin-top: 6px;
    justify-content: flex-end;
}
.sc-priority-badge {
    width: 28px;
    height: 28px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;
    font-weight: 700;
    background: rgba(255,255,255,0.2);
    color: #fff;
    border: 2px solid rgba(255,255,255,0.4);
}

/* Categories */
.sc-category {
    background: #fff;
    border-radius: 10px;
    border: 1px solid #e5e7eb;
    margin-bottom: 12px;
    overflow: hidden;
}
.sc-cat-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 12px 16px;
    cursor: pointer;
    user-select: none;
    border-bottom: 1px solid #f3f4f6;
    background: #fafafa;
}
.sc-cat-header:hover { background: #f3f4f6; }
.sc-cat-header-left {
    display: flex;
    align-items: center;
    gap: 10px;
}
.sc-cat-label {
    font-size: 0.95rem;
    font-weight: 600;
    color: #c2410c;
}
.sc-cat-count {
    font-size: 0.78rem;
    color: #6b7280;
    background: #f3f4f6;
    padding: 2px 8px;
    border-radius: 999px;
}
.sc-cat-chevron {
    font-size: 0.75rem;
    color: #6b7280;
}

/* Signal rows */
.sc-signal-row {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: 12px 16px;
    border-bottom: 1px solid #f9fafb;
}
.sc-signal-row:last-child { border-bottom: none; }

/* Checkbox */
.sc-checkbox {
    width: 20px;
    height: 20px;
    border-radius: 4px;
    border: 2px solid #d1d5db;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    flex-shrink: 0;
    margin-top: 2px;
    font-size: 0.7rem;
    color: #fff;
    background: #fff;
    transition: all 0.15s;
}
.sc-checkbox-checked {
    background: #22c55e;
    border-color: #22c55e;
}

/* Signal info */
.sc-signal-info { flex: 1; overflow: hidden; }
.sc-signal-name {
    margin: 0 0 2px;
    font-size: 0.88rem;
    font-weight: 500;
    color: #111827;
}
.sc-signal-desc {
    margin: 0;
    font-size: 0.78rem;
    color: #6b7280;
}

/* Controls */
.sc-signal-controls {
    display: flex;
    gap: 8px;
    align-items: center;
    flex-shrink: 0;
}
.sc-select {
    padding: 5px 8px;
    font-size: 0.78rem;
    border-radius: 6px;
    border: 1px solid #d1d5db;
    background: #fff;
    cursor: pointer;
}
.sc-select-priority {
    min-width: 130px;
    border-color: #fca5a5;
    background: #fff7f7;
    color: #b91c1c;
}
.sc-select-timeframe {
    min-width: 110px;
}