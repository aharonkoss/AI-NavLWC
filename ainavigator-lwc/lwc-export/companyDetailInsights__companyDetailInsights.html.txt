<!--
  @description       : 
  @author            : ChangeMeIn@UserSettingsUnder.SFDoc
  @group             : 
  @last modified on  : 03-27-2026
  @last modified by  : ChangeMeIn@UserSettingsUnder.SFDoc
-->
<template>
    <div class="cdi-wrapper">

        <!-- Loading -->
        <template if:true={isLoading}>
            <div class="cdi-loading">
                <span class="cdi-spinner"></span>
                <span>Loading insights...</span>
            </div>
        </template>

        <template if:false={isLoading}>

            <!-- Company Profile Card -->
            <div class="cdi-profile-card">
                <h3 class="cdi-section-title">Company Profile</h3>
                <div class="cdi-profile-row">
                    <span class="cdi-profile-label">Business Type</span>
                    <span class="cdi-profile-value">{companyProfile.businessType}</span>
                </div>
                <div class="cdi-profile-row">
                    <span class="cdi-profile-label">Key Characteristics</span>
                    <span class="cdi-profile-value">{companyProfile.keyCharacteristics}</span>
                </div>
            </div>

            <!-- Call Strategy Card -->
            <div class="cdi-strategy-card">
                <h3 class="cdi-section-title">Call Strategy</h3>
                <div class="cdi-strategy-objective">
                    <span class="cdi-profile-label">Primary Objective</span>
                    <p class="cdi-profile-value">{callStrategy.primaryObjective}</p>
                </div>
                <div class="cdi-strategy-cols">
                    <div>
                        <span class="cdi-profile-label">Key Focus Areas</span>
                        <ul class="cdi-bullet-list">
                            <template for:each={callStrategy.keyFocusAreas} for:item="area">
                                <li key={area} class="cdi-bullet-item">
                                    <span class="cdi-bullet-dot">·</span>{area}
                                </li>
                            </template>
                        </ul>
                    </div>
                    <div>
                        <span class="cdi-profile-label">Follow-up Actions</span>
                        <ul class="cdi-bullet-list">
                            <template for:each={callStrategy.followUpActions} for:item="action">
                                <li key={action} class="cdi-bullet-item">
                                    <span class="cdi-bullet-dot">·</span>{action}
                                </li>
                            </template>
                        </ul>
                    </div>
                </div>
                <div class="cdi-strategy-approach">
                    <span class="cdi-profile-label">Relationship Approach</span>
                    <p class="cdi-profile-value">{callStrategy.relationshipApproach}</p>
                </div>
            </div>

            <!-- Insight Count Summary -->
            <div class="cdi-count-row">
                <span class="cdi-count-label">{insightCount} Insights</span>
                <div class="cdi-badge-row">
                    <span class="cdi-badge cdi-badge-green">{actionableCount} Actionable</span>
                    <span class="cdi-badge cdi-badge-purple">{strategicCount} Strategic</span>
                    <span class="cdi-badge cdi-badge-orange">{relationalCount} Relational</span>
                </div>
            </div>

            <!-- Insight Cards Grid -->
            <div class="cdi-grid">
                <template for:each={insights} for:item="insight">
                    <div key={insight.title} class="cdi-insight-card">

                        <!-- Card Header -->
                        <div class="cdi-card-header">
                            <div class="cdi-badge-row">
                                <span class={insight.categoryClass}>{insight.category}</span>
                                <span class={insight.priorityClass}>{insight.priority}</span>
                                <span class={insight.recencyClass}>R:{insight.recency}</span>
                                <span class={insight.reliabilityClass}>L:{insight.reliability}</span>
                            </div>
                        </div>

                        <!-- Title -->
                        <h4 class="cdi-insight-title">{insight.title}</h4>

                        <!-- Data Trigger -->
                        <div class="cdi-trigger-box">
                            <span class="cdi-trigger-label">Data Trigger</span>
                            <span class="cdi-trigger-text">{insight.rawDataTrigger}</span>
                        </div>

                        <!-- Insight Analysis -->
                        <p class="cdi-insight-body">{insight.insight}</p>

                        <!-- Business Need -->
                        <div class="cdi-need-row">
                            <span class="cdi-profile-label">Business Need  </span>
                            <span class="cdi-need-text">{insight.businessNeed}</span>
                        </div>

                        <!-- Discovery Questions -->
                        <template if:true={insight.hasQuestions}>
                            <div class="cdi-questions">
                                <span class="cdi-profile-label">Discovery Questions</span>
                                <ul class="cdi-q-list">
                                    <template for:each={insight.discoveryQuestions} for:item="q">
                                        <li key={q} class="cdi-q-item">{q}</li>
                                    </template>
                                </ul>
                            </div>
                        </template>

                        <!-- Advisory Opportunity -->
                        <div class="cdi-advisory-box">
                            <span class="cdi-advisory-label">Advisory Opportunity</span>
                            <p class="cdi-advisory-text">{insight.advisoryOpportunity}</p>
                        </div>

                        <!-- Expected Outcome -->
                        <div class="cdi-outcome-box">
                            <span class="cdi-outcome-label">Expected Outcome</span>
                            <p class="cdi-outcome-text">{insight.expectedOutcome}</p>
                        </div>

                    </div>
                </template>
            </div>

        </template>
    </div>
</template>