:host {
    display: block;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

/* ── Wrapper ─────────────────────────────────────────────────────────────── */
.coach-wrapper {
    background: #ffffff;
    border-radius: 12px;
    border: 1px solid #e5e7eb;
    overflow: hidden;
}

/* ── Header ──────────────────────────────────────────────────────────────── */
.coach-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px 18px;
    background: linear-gradient(to right, #fffbeb, #fef3c7);
    border-bottom: 1px solid #fde68a;
}
.coach-header-left { flex: 1; }
.coach-title {
    font-size: 15px;
    font-weight: 700;
    color: #111827;
    margin: 0;
}
.coach-subtitle {
    font-size: 11px;
    color: #6b7280;
    margin: 3px 0 0;
}
.coach-header-right {
    display: flex;
    align-items: center;
    gap: 12px;
}
.coach-score {
    font-size: 12px;
    color: #6b7280;
    white-space: nowrap;
}
.btn-reset {
    padding: 4px 12px;
    font-size: 11px;
    background: #f3f4f6;
    color: #4b5563;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    cursor: pointer;
    transition: background 0.15s;
}
.btn-reset:hover { background: #e5e7eb; }

/* ── Category Filter Pills ───────────────────────────────────────────────── */
.coach-filters {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    padding: 10px 18px;
    background: #f9fafb;
    border-bottom: 1px solid #f3f4f6;
}
.filter-btn {
    padding: 5px 14px;
    font-size: 11px;
    border-radius: 999px;
    border: 1px solid #e5e7eb;
    background: #ffffff;
    color: #6b7280;
    cursor: pointer;
    transition: all 0.15s;
    white-space: nowrap;
}
.filter-btn:hover {
    background: #fef9c3;
    border-color: #fbbf24;
    color: #92400e;
}
.filter-btn--active {
    background: #f59e0b;
    border-color: #f59e0b;
    color: #ffffff;
    font-weight: 600;
}

/* ── Body ────────────────────────────────────────────────────────────────── */
.coach-body { padding: 0; }

/* ── Empty State ─────────────────────────────────────────────────────────── */
.coach-empty {
    padding: 52px 24px;
    text-align: center;
    color: #9ca3af;
    font-size: 13px;
}
.coach-empty-sub {
    font-size: 11px;
    margin-top: 6px;
    color: #d1d5db;
}

/* ── Quiz Inner ──────────────────────────────────────────────────────────── */
.quiz-inner { padding: 18px; }

/* Progress row */
.quiz-progress {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 14px;
    font-size: 11px;
    color: #9ca3af;
}

/* Type badge pill */
.type-badge {
    padding: 2px 10px;
    border-radius: 999px;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.4px;
}
.type-badge--updates { background: #dbeafe; color: #1d4ed8; }
.type-badge--signals { background: #ede9fe; color: #5b21b6; }
.type-badge--company { background: #d1fae5; color: #065f46; }

/* Question box */
.quiz-question-box {
    background: #f9fafb;
    border: 1px solid #f3f4f6;
    border-radius: 10px;
    padding: 16px 18px;
    margin-bottom: 16px;
}
.quiz-question-text {
    font-size: 13px;
    font-weight: 600;
    color: #111827;
    line-height: 1.65;
    margin: 0;
}

/* ── Answer Options ──────────────────────────────────────────────────────── */
.quiz-options {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 16px;
}
.opt-btn {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    width: 100%;
    text-align: left;
    padding: 11px 14px;
    border-radius: 8px;
    border: 1px solid #e5e7eb;
    background: #ffffff;
    font-size: 12px;
    color: #374151;
    cursor: pointer;
    line-height: 1.5;
    transition: border-color 0.15s, background 0.15s;
}
.opt-btn:hover:not([disabled]) {
    background: #f9fafb;
    border-color: #d1d5db;
}
.opt-btn[disabled] { cursor: default; }

/* Correct answer */
.opt-btn--correct {
    background: #f0fdf4 !important;
    border-color: #86efac !important;
    color: #166534 !important;
}
/* Wrong answer selected */
.opt-btn--wrong {
    background: #fef2f2 !important;
    border-color: #fca5a5 !important;
    color: #991b1b !important;
}

/* Option circle letter */
.opt-dot {
    min-width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 1.5px solid #d1d5db;
    background: #ffffff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    font-weight: 700;
    color: #6b7280;
    flex-shrink: 0;
    margin-top: 1px;
}
.opt-dot--correct {
    background: #22c55e;
    border-color: #22c55e;
    color: #ffffff;
}
.opt-dot--wrong {
    background: #ef4444;
    border-color: #ef4444;
    color: #ffffff;
}
.opt-text { flex: 1; }

/* ── Coach Tip Box ───────────────────────────────────────────────────────── */
.coach-tip {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    background: #eff6ff;
    border: 1px solid #bfdbfe;
    border-radius: 8px;
    padding: 12px 14px;
    margin-bottom: 8px;
}
.tip-icon {
    width: 26px;
    height: 26px;
    min-width: 26px;
    background: #3b82f6;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
}
.tip-svg {
    width: 14px;
    height: 14px;
}
.tip-label {
    font-size: 10px;
    font-weight: 700;
    color: #1e40af;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin: 0 0 4px;
}
.tip-body {
    font-size: 12px;
    color: #1d4ed8;
    margin: 0;
    line-height: 1.55;
}

/* ── Context Box ─────────────────────────────────────────────────────────── */
.coach-context {
    background: #f3f4f6;
    border: 1px solid #e5e7eb;
    border-radius: 8px;
    padding: 10px 14px;
    font-size: 11px;
    color: #6b7280;
    margin-bottom: 16px;
    line-height: 1.5;
}
.context-label {
    font-weight: 700;
    color: #4b5563;
}

/* ── Navigation ──────────────────────────────────────────────────────────── */
.quiz-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-top: 10px;
    border-top: 1px solid #f3f4f6;
}
.btn-prev,
.btn-next {
    padding: 7px 20px;
    font-size: 12px;
    font-weight: 500;
    border-radius: 7px;
    border: none;
    cursor: pointer;
    transition: all 0.15s;
}
.btn-prev {
    background: #f3f4f6;
    color: #374151;
    border: 1px solid #e5e7eb;
}
.btn-prev:hover:not([disabled]) { background: #e5e7eb; }
.btn-next {
    background: #f59e0b;
    color: #ffffff;
    font-weight: 600;
}
.btn-next:hover:not([disabled]) { background: #d97706; }
.btn-prev[disabled],
.btn-next[disabled] {
    opacity: 0.35;
    cursor: not-allowed;
}